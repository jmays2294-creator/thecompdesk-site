# Preflight — 20260805190000_attorney_directory.sql — PASS

**Date:** 2026-08-05 19:05:00
**File:** `.../attorney-directory/supabase/migrations/20260805190000_attorney_directory.sql`
**Project:** ltibymvlytodkemdeeox
**Verdict:** SAFE TO APPLY

## Check 1 — Postgres-syntax compliance (MySQL COMMENT trap): PASS
No inline `COMMENT '...'` on any ADD COLUMN / column definition. All commentary uses
separate `comment on table|function ... is '...'` statements.

## Check 2 — File-number collision: PASS
Local intent is monorepo number **111**. `list_migrations` shows the highest applied
prefix is `110_handle_new_user_profession` (version 20260805210620). No `111_*` exists.
111 is free, re-verified at write time as required.

## Check 3 — RLS hygiene: PASS
- No bare `auth.uid()`. The single occurrence is wrapped: `(select auth.uid())` inside
  `owns_directory_chat`.
- No policy reads another RLS-protected table directly. Every cross-table lookup
  (directory_chats -> directory_profiles) goes through the SECURITY DEFINER helper
  `owns_directory_chat`, per the 42P17 recursion lesson of April 2026.
- RLS enabled on all four new tables.

## Check 4 — Tombstone banner: PASS
No SUPERSEDED / DO NOT APPLY banner; no conditional approval gate.

## What this preflight did NOT cover
- Whether the migration's intent matches the spec (read by hand against the brief; the
  extend-vs-new-table decision is argued in the file header from live evidence:
  attorney_accounts has 0 rows and 19 columns, ~10 of ~30 needed).
- Whether columns/tables it adds already exist under different names in prod — checked
  manually: no `directory_*` relation exists.
- CHECK constraints against existing rows — not applicable, all four tables are new.
- Edge functions depending on the new schema — not yet written (see report).
- The GRANT decision is argued but must be confirmed post-apply against `pg_proc.proacl`.
