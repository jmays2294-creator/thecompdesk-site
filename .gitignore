# macOS
.DS_Store
**/.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example

# Node
node_modules/

# Sweep artifacts — regenerated every night, never reviewed as a diff
tests/workspace-sweep/reports/
